package dk.in2isoft.onlineobjects.core.exceptions;

public enum Error {
	emailExists, invalidEmail, emailSameAsCurrent, userExists, noEmail, invalidPassword, noUsername, invalidUsername, noPassword, missingCurrentPassword, missingNewPassword, incorrectCurrentPassword, invalidNewPassword, unableToSendMails, userNotFound, userNotCurrent
}
